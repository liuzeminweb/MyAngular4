import { Drag1Directive } from './drag1.directive';

describe('Drag1Directive', () => {
  it('should create an instance', () => {
    const directive = new Drag1Directive();
    expect(directive).toBeTruthy();
  });
});
