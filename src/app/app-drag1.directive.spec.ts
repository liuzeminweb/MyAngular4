import { AppDrag1Directive } from './app-drag1.directive';

describe('AppDrag1Directive', () => {
  it('should create an instance', () => {
    const directive = new AppDrag1Directive();
    expect(directive).toBeTruthy();
  });
});
