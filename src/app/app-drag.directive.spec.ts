import { AppDragDirective } from './app-drag.directive';

describe('AppDragDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDragDirective();
    expect(directive).toBeTruthy();
  });
});
